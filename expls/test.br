class tests
    function add(x, y)
        println("x: " + Str(x) + " y: " + Str(y))
        return x + y  # Corrigido para usar x e y
    end
end

struct test
    add
end

a = tests()
#a.add = function (x, y)
#    return x + y
#end

print(a.add)